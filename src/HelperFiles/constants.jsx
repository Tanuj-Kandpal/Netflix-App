//Add search functionality
//Link Tag

//Redux toolkit
//When we click add icon it will dispatch an a function which is called reducer function which will add item to the cart/ (update the slice of our redux store).
export const image_baseuri = `https://image.tmdb.org/t/p/original`;

export const randomId = Math.floor(Math.random() * 100000);
